#include <stdio.h>

int main() {
    int sum=0,n;
      printf("Enter the number of terms (n) :");
scanf("%d", &n);
    for (int i = 0; i <=0; i++)
    {
 
        sum=n*n;
        printf(" THe sum of %d",sum);
    }
    

    return 0;
}
